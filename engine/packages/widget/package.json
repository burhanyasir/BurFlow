{
  "name": "@conversation-engine/widget",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "browser": "dist/widget.js",
  "scripts": {
    "build": "tsc && npx esbuild --bundle --target=es2022 --format=iife --minify --sourcemap --outfile=dist/widget.js src/index.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "vitest": "^2.0.0"
  }
}
