version: "3.9"

services:
  brightsmile:
    build: .
    container_name: brightsmile-ai
    ports:
      - "3456:3456"
    environment:
      - NODE_ENV=production
      - PORT=3456
      - LOG_LEVEL=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3456/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # Uncomment for persistent storage:
    # volumes:
    #   - brightsmile-data:/app/data
    # depends_on:
    #   - postgres

  # Uncomment when PostgreSQL is needed:
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: brightsmile-db
  #   environment:
  #     - POSTGRES_DB=brightsmile
  #     - POSTGRES_USER=brightsmile
  #     - POSTGRES_PASSWORD=change-me
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   brightsmile-data:
#   pgdata:
